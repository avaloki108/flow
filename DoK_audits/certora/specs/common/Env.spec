/**
 * Common environment helpers (time, msg.sender assumptions etc).
 * Keep minimal; add only what you need for counterexamples.
 */
spec Env {
  // placeholder for shared definitions; expand per repo.
}